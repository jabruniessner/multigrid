#include <cassert>
#include <random>

#ifndef FUNCTIONS_H
#define FUNCTIONS_H

using DataType = double;

void initialize_random(DataType *, DataType *, int);
void compare(DataType *, DataType *, int);

#endif
